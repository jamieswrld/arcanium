{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "corepack enable && pnpm install --frozen-lockfile=false && pnpm --filter @arch/indexer... build"
  },
  "deploy": {
    "startCommand": "node scripts/run-arc-services.mjs",
    "restartPolicyType": "ALWAYS",
    "restartPolicyMaxRetries": 10
  }
}
