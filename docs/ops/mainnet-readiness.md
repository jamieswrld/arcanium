# Arch — Mainnet Readiness Checklist

Status as of 2026-07-29. Everything below the line is BUILT AND PROVEN on real
testnets; the gate items are what stand between tonight's system and real
funds. `ENABLE_ARC_MAINNET` / `ENABLE_REAL_BRIDGE` / `ENABLE_REAL_TRADING`
stay false until every gate passes.

## Built and verified (testnet, real transactions)

- [x] Bridge: vault (Base Sepolia) + aUSD + bridge (Arc testnet); deposit→mint and burn→release executed end-to-end; reserve == supply proven live; 81/81 Foundry tests incl. adversarial invariants.
- [x] Autonomous workers processing real deposits/redemptions (replay-proof on-chain).
- [x] Gas station: live quote → gas-free permit → relayer drip on the production site.
- [x] Uniswap v3 (official bytecode) deployed to Arc testnet; launchpad factory/vault/distributor/graduation deployed; ARCHIE launched with atomic creator buy; fees flowing to the 7-wallet splitter (40/10×6) on both chains.
- [x] Migration: 1:1 exchange (cannot open underwater, no deadline) + pool migrator (price/value preservation proven in tests, both orderings).
- [x] Frontend: wallet-connected bridge, gas, discovery, create, token page with live chart/trades — all data live from chain, deployed on Vercel.

## Gates before real funds

1. [ ] **Arc mainnet parameters verified from official Circle/Arc sources** (chain id, RPC, explorer, USDC, finality). Evidence points to 5042 but official docs still said testnet-only when checked.
2. [ ] **Uniswap v3 on Arc mainnet**: use the official/canonical deployment if one exists (chain-5042 addresses observed: PM `0x3965…1377`, router `0x4C91…4720` — verify against Uniswap's official deployments registry); deploy from official artifacts only if none is canonical.
3. [ ] **Key ceremony**: every key generated this session is burned for mainnet (they exist in session logs). Fresh keys: deployer, Base keeper, Arc keeper, relayer — hardware/KMS. The 7 fee wallets regenerated BY THE OWNER in their own wallet app; splitter recipients swapped via `setRecipients`.
4. [ ] **Role separation + multisig + timelock**: protocol multisig (e.g. Safe) owns every contract via `acceptOwnership`; timelock in front of fee/config changes; separate treasuries per stream; relayer/keepers hold float only.
5. [ ] **Security review**: minimum one independent senior review of vault/bridge/vault-lock paths (see cost sheet tiers); fix + re-test.
6. [ ] **Capped beta parameters**: launch with conservative `maxDeposit`, release/mint windows, and gas-station caps; raise gradually.
7. [ ] **Ops**: workers + API on a VPS (not a laptop); dedicated RPC endpoints; monitoring alerts on solvency, worker lag, relayer balance; incident runbook rehearsed (pause drill executed once on testnet before launch).
8. [ ] **Persistent indexer**: Postgres-backed indexer (schema ready) deployed so charts/trades/holders have full history; UI trades/holders currently use bounded recent windows.
9. [ ] **Launch fee restored to mainnet value**: `LAUNCH_FEE_QUOTE_UNITS=52500000` (52.5 = 105% of Envelope's verified 50); testnet runs at 5 aUSD for wallet-budget reasons.
10. [ ] **Disclosures live**: docs pages with full aUSD/fee/risk disclosures (skeleton exists at /docs); contract addresses page updated with mainnet deployments + explorer verification of all sources.

## Deployment order (mainnet day)

1. Base: vault (fee 1500, min 25, max capped-beta) + fee splitter → verify on Basescan.
2. Arc: aUSD → bridge (grant BRIDGE_ROLE) → gas station (fund inventory) → splitter → launchpad stack → wire treasuries → verify all.
3. Transfer ownerships to multisig (two-step, accept from multisig).
4. Start workers against mainnet config; run one $25 canary deposit → mint → redeem → release.
5. Flip `ENABLE_REAL_BRIDGE=true` on the site; announce controlled beta.
